# Test API

Su dung `test.http` de goi nhanh cac endpoint cua backend Django moi.

Luu y:

- Endpoint tao ban tom tat can dang nhap neu dung trong trinh duyet.
- Neu dung client API, can gui CSRF va session neu muon test day du luong auth cua Django.
